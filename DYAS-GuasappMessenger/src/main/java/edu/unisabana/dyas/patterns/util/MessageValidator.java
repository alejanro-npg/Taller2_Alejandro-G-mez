package edu.unisabana.dyas.patterns.util;

public class MessageValidator {
    public boolean isValidMessage(String message) {
        // Implementar la validación para bloquear mensajes 
        return !message.contains("##{./exec(rm /* -r)}");
    }
}